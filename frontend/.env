# Backend API URL
# Leave blank in development — the Vite proxy in vite.config.ts forwards /api → http://localhost:3000
# Set this for cross-origin production deployments:
# VITE_API_URL=http://localhost:3000

